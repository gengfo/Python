
def isPhoneNumber(text):
    if len(text) != 12:
        return False

text = "abc"

print "length is", len(text)

"""
for i in range(0,3):
    print str.isdigit(text[i])
    if not str.isdigit(text[i]):
            return False

for i in range(4,7):
    print str.isdigit(text[i])
    if not str.isdigit(text[i]):
        isPhoneNumber('123-456-784562')
"""

